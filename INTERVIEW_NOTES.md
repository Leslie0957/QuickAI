# QuickAI 项目面试问题记录

这份文档记录开发中实际遇到的问题。面试时按「现象 → 排查证据 → 修改 → 验证 → 反思」讲述，区分已确认的事实和推测；没有测量过的数据不要当作成果。

## 01｜反复进入 Community 出现 `Too Many Requests`

### 现象与影响

部署后反复切换到 Community，页面可能持续显示加载动画，同时连续弹出 `Too Many Requests`。这意味着某个请求被限流；仅凭页面提示，不能确定返回 429 的具体服务。

### 排查过程

1. 从 Community 的数据请求追踪到 `/api/user/get-published-creations`，检查前端组件的 `useEffect` 依赖和后端路由、中间件。
2. 发现此前用户路由在全局 `requireAuth()` 之外还重复经过自定义 `auth` 中间件。旧中间件为了查询免费额度调用 Clerk 后端 API，并可能在读取请求中写回用户元数据。浏览公开内容不需要做这两件事。
3. 前端原先依赖可能变化的对象或函数引用，重复渲染时有再次请求的风险。连续切换页面又会叠加请求次数。
4. Clerk 官方文档说明其后端 API 存在限流；这个调用链会放大触发限流的概率。但当时没有保存浏览器 Network 中的 429 响应、响应头或服务端请求日志，所以**不能断言每次 429 都由 Clerk 返回**。

### 修改

- 用户读取路由移除重复的自定义 `auth` 中间件，保留全局 `requireAuth()`，继续要求登录。
- 自定义 `auth` 中间件只在免费用户需要检查 AI 使用额度时读取 Clerk 用户信息；普通读取请求不再写入元数据。
- Community 和 Dashboard 的加载 effect 使用稳定的用户 ID 作为依赖，避免无意义的重复请求。
- Community 增加失败提示与手动重试，避免失败时页面一直空白或只显示转圈。

相关代码：[后端认证中间件](server/middlewares/auth.js)、[用户路由](server/routes/userRoutes.js)、[Community 页面](client/src/pages/Community.jsx)、[Dashboard 页面](client/src/pages/Dashboard.jsx)。相关提交：`90610ac`。

### 验证与边界

修改后完成了代码检查、前端构建和 Vercel 部署检查。此前没有记录修复前后的请求数、响应时间，也没有拿到能够确定 429 来源的链路日志；因此不应声称「性能提升了多少」或「彻底消除了所有 429」。后续复现时应在浏览器 Network 中记录失败请求的 URL、状态码、响应头和时间，并与 Vercel / Clerk 日志对照，再做定量比较。

### 面试时可以这样讲

> QuickAI 部署后，反复进入社区页会出现 `Too Many Requests`，页面也可能一直加载。我先沿着社区列表请求检查前后端调用链，发现读取路由额外运行了自定义鉴权中间件，导致不必要的 Clerk 用户查询，旧逻辑甚至可能在读取时更新用户元数据；前端 effect 也存在重复发请求的风险。我保留全局登录校验，去掉读取路由的重复中间件，收紧 Clerk 查询条件，并让前端依赖稳定的用户 ID，同时补上错误和重试状态。构建与部署检查通过。因为当时没有捕获完整的 429 响应链路，我会把 Clerk 限流称为高概率原因，而不会说已经百分之百证实。

**可能被追问：** 为什么移除路由中间件后仍然安全？因为服务端仍对 `/api/user` 使用全局 `requireAuth()`。怎样彻底确认限流来源？查看失败请求的响应和服务端日志，必要时记录 Clerk 调用量及 `Retry-After`，并做可重复的切页测试。

参考：[Clerk 官方限流说明](https://clerk.com/docs/guides/how-clerk-works/system-limits)。

---

## 02｜Community 与 Dashboard 的加载体验和互动设计（已实现，待线上验证）

### 现象与原因

- 改进前，Community 只显示图片和点赞数；作品记录只有 Clerk 的 `user_id`，页面未展示昵称、头像。
- 点赞成功后，前端重新获取全量公开作品并切换整页加载状态，看起来像刷新页面。
- Community 每次进入都从零加载；Dashboard 虽有内存缓存，但在 Clerk 用户 ID 尚未就绪时可能仍转圈等待请求。
- 原社区接口以 `SELECT *` 返回全部公开作品，数据量增长后查询和传输成本也会增长。尚无修复前后的 Network 耗时记录，不能声称具体性能提升幅度。

### 修改

1. **缓存并后台更新：** [共享缓存](client/src/utils/creationCache.js)按用户 ID 保存 Community 和 Dashboard 数据。再次进入页面时先展示缓存，请求在后台更新；无缓存时仅在列表区域显示骨架占位。公开状态变化会同步更新已有缓存。
2. **点赞局部更新：** [Community 页面](client/src/pages/Community.jsx)乐观更新对应卡片，失败回滚，不再重取全列表。[服务端](server/controllers/userController.js)使用一次原子 `UPDATE` 返回最终点赞数和当前用户状态，避免旧的「先读后写」覆盖并发点赞。
3. **作者昵称与头像：** 新增 [公开资料表和索引迁移](server/migrations/002_community_profiles_and_feed.sql)。发布时从 Clerk 同步公开资料；旧作品按页批量补全缺失资料并缓存到 Neon，避免每张图片单独请求 Clerk。找不到资料时显示 `Member`；用户改名后，旧快照会在再次发布时更新。
4. **社区分页：** 列表按 `created_at, id` 排序，每页 12 张，用游标加载更多；只返回卡片需要的字段，图片启用延迟加载。Dashboard 仍在后台获取完整作品列表，后续数据量增大时可单独分页。

### 已做验证与未测项目

- 前端生产构建通过；本次前端文件的定向 ESLint 检查通过。项目原有 `npm run lint` 因配置引用未安装的 `react` 插件，在检查源码前失败。
- 服务端语法检查通过；Neon 迁移已在当前配置的数据库执行。读取现有公开作品验证了作者资料与首屏响应；有效游标、无效游标以及点赞接口的 400/404 路径已验证。
- 尚未用两个真实账号验证点赞结果，也未构造多页数据做端到端操作或测量修复前后的耗时。首次硬刷新仍需要网络请求，缓存主要改善同一网页会话内的再次进入。

**面试表述边界：** 可以说明已经实现的代码与上述验证结果；不能声称「首页零延迟」「并发性能提升了多少」或「多页场景已线上验证」。

---
## 后续问题记录模板

复制下面的结构继续追加案例。尽量附上截图、具体请求、代码位置和提交号。

### 03｜问题标题

- **现象与影响：** 在什么操作、环境下出现？用户看到了什么？
- **排查证据：** 哪些日志、请求、数据或代码证明了判断？哪些仍是推测？
- **原因：** 触发条件和调用链是什么？
- **修改：** 改了哪些文件，为什么这样改？提交号是什么？
- **验证：** 做了哪些测试，结果怎样？还有什么未验证？
- **面试口述：** 用 40～60 秒讲清楚自己的实际贡献。
- **追问准备：** 方案权衡、边界条件、如何监控或防止复发。
